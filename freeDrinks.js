var burgerPrice = 500;
var cokePrice = 30;
if (burgerPrice > 500){

    cokePrice = 0;
    payAmount = burgerPrice + cokePrice
    console.log(payAmount)
}

    else{
    payAmount = burgerPrice + cokePrice
    console.log(payAmount)
}
    